<script>
    var isWeChat = false;
    is_weChat();

    // 判断是否微信浏览器
    function is_weChat(){
        var ua = navigator.userAgent.toLowerCase();
        if(ua.match(/MicroMessenger/i) == "micromessenger") {
            isWeChat = true;
        }
    }
</script>
$screen_content
